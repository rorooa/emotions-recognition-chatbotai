{
    "name": "emotion-ai-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
        "backend": "cd backend && .\\venv2\\Scripts\\python -m uvicorn main:app --reload --port 8000",
        "frontend": "cd myapp && npm run dev"
    },
    "devDependencies": {
        "concurrently": "^8.0.0"
    }
}